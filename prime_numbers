function filterPrimeNumbers(array){


    // function isPrime(arr){
    //     for (let i=0;i<arr.length;i++){
    //         if ((arr[i]%1==0)&&(arr[i])%arr[i]){
    //             return arr[i];
    //         }
    //         return arr[i]
    //     }
    
    // }
    let filtered= array.filter(function (element){

        
        return element%1==0 && element%element==0;
    })
return filtered;
}

array=[4, 7, 12, 11, 15, 17, 20, 23]
console.log(filterPrimeNumbers(array));
